---
interface Props {
  as?: 'div' | 'section' | 'article';
  class?: string;
}

const { as: Tag = 'div', class: className = '' }: Props = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-7xl px-5 sm:px-8 lg:px-12', className]}>
  <slot />
</Tag>
